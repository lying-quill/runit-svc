# shellcheck disable=SC2034

pkgname=foo
pkgdesc='opinionated service template for runit'
pkgver=1.0.0
url=https://github.com/lying-quill/runit-svc
arch=(any)
license=MIT
# should i also mention base-devel? as i'm using gnu make for installation?
depends=(
    'bash>=5.3.15'
    'coreutils>=9.11'
    'runit>=2.3.1'
)
